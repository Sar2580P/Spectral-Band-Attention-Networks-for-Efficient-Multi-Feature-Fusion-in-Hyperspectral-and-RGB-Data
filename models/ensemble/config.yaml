dir : 'models/ensemble/logs'

num_classes : 96

BATCH_SIZE : 64
lr : 0.000021

in_channels : 168
weight_decay : 0.000001
ckpt_file_name : 'ensemble--{epoch}-{val_loss:.2f}-{val_accuracy:.2f}'
model_name : 'rice_ensemble'
